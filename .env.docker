# MySQL
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=authdb
MYSQL_USER=authuser
MYSQL_PASSWORD=authpass

# MongoDB
MONGO_INITDB_DATABASE=miniprojet

# Auth Service
AUTH_PORT=3060
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
DB_HOST=mysql
DB_PORT=3306
DB_USER=authuser
DB_PASSWORD=authpass
DB_NAME=authdb

# Gateway
PORT=8000
AUTH_SERVICE_URL=http://auth:3060
BACKEND_SERVICE_URL=http://backend:5000
FRONTEND_URL=http://frontend:3000

# Backend (NestJS)
PORT=5000
MONGODB_URI=mongodb://mongodb:27017/miniprojet

# Frontend (Next.js)
PORT=3000
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000